<%= provide(:title, "投稿一覧") %>

<div class="container">
  <div class="post_index">
    <div class="post_device">
      <% if current_user && current_user.skill == true %>

      <% else %>
       <%= render 'host_user_index'%>
      <% end %>
    </div>
  </div>
</div>
