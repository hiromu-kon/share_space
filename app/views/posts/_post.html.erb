<% if post.user.skill == false %>
  <hr class="post_separate">
  <div class="post_user_area">
    <%= link_to post.user do %>
      <div class="post_company">
        <%= render 'shared/icon_default', user: post.user %>
        <span><%= post.user.name %></span>
      </div>
    <% end %>
  </div>
  <div class="post_area">
    <%= link_to post do %>
      <div>
        <% if post.image.attached? %>
          <%= image_tag post.image, class:"post_index_image" %>
        <% end %>
      </div>
      <h1 class="post_title"><%= post.title %></h1>
      <p class="post_content"><%= truncate(post.content, length: 90) %></p>
      <div class="group"><h3>報酬(月単価)<span>約<%= number_to_currency(post.reward) if post.reward? %></span></h3></div>
      <div class="group"><h3>募集人数<span><%= post.recruit_people %>人</span></h3></div>
      <div class="group"><h3>募集期間<span><%= post.start_date %> 〜 <%= post.finish_date %></span></h3></div>
      <h3 class="post_address">オフィス住所<span><%= post.map&.address %>
      </span></h3>
      <div class="post_index_tag">
        <% post.tags.each do |tag| %>
          <span><%= tag.name %></span>
        <% end %>
      </div>
    <% end %>
  </div>
<% end %>
