<span id="unbookmark_<%= post.id %>">
  <%= link_to post_bookmark_path(post.id, Bookmark.find_by(post_id: post.id, user_id: current_user.id)),
    method: :delete, remote: true, class: "btn-border_reverse" do %>
    <i class="far fa-bookmark"></i> ブックマークをはずす
  <% end %>
</span>
