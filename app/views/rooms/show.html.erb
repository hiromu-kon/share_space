<% provide(:title, "メッセージ") %>

<div class="post_index">
  <div class="room_show_box">
    <% @entries.each do |e| %>
      <% if e.user.id != current_user.id %>
        <%= link_to user_path(e.user.id) do %>
          <div class="company_room">
            <%= render 'shared/icon_default', user: e.user %>
            <span>
              <%= e.user.name %>さん
              <% if e.user.skill == true %>
                <p class="skill_company"><i class="far fa-check-circle"></i>スキル企業</p>
              <% end %>
            </span>
          </div>
        <% end %>
      <% end %>
    <% end %>
    <hr class="company_chat">
    <div id="messages" class="row">
      <% @messages.each do |message| %>
        <%= render partial: 'messages/message', locals: {message: message} %>
      <% end %>
    </div>
  </div>
  <div id="message_error"></div>
  <%= render "chat_form" %>
</div>
